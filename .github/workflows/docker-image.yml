name: Docker Build And Push To Aliyun Hub

on:
  push:
    branches:
      - dev

jobs:
  build:
    name: Build Spring Boot
    runs-on: ubuntu-latest
    steps:
      - name: Git Checkout Code
        uses: actions/checkout@v1
        id: git_checkout

      - name: Build Docker Image
        id: buildAndPushImage
        uses: risfeng/docker-image-build-push-action@v1.0
        with:
          registry_url: 'weiming-swimming-docker.pkg.coding.net'
          namespaces: 'zhibo-brusherzhibo-brusher/screenshot-to-code'
          repository_name: 'weiming-swimming-docker.pkg.coding.net'
          user_name: 'ptlwwl5h79i5'
          password: '7821a7a44c18db852c57025846d28e4fe42a50fb'
          image_version: 'v1.0'
          docker_file: '.'
      - name: Get pre step result output image_pull_url
        run: echo "The time was ${{ steps.buildAndPushImage.outputs.image_pull_url }}"
